* Implement all actuator register definitions
* Defin dxl_* functions in dxl.h
* Implement read and write function, if possible in bulk mode, with
devicename, baudrate, id start, id stop, address, size, sign
